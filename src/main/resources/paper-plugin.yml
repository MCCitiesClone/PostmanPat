name: PostmanPat
version: '2.0.0'
main: me.zodd.postmanpat.PostmanPat
description: Postman
api-version: '1.21.1'
loader: me.zodd.postmanpat.ExternalDependencyLoader
dependencies:
  server:
    Essentials:
      load: BEFORE
      required: true
    DiscordSRV:
        load: BEFORE
        required: true
    Vault:
        load: BEFORE
        required: true
    DemocracyBusiness:
        load : BEFORE
        required: false
    AreaShop:
        load: BEFORE
        required: false
    LiteBans:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      require: false
