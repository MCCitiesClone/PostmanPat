name: PostmanPat
version: '${version}'
main: me.zodd.postmanpat.PostmanPat
description: Postman
api-version: '1.21.1'
loader: me.zodd.postmanpat.ExternalDependencyLoader
dependencies:
  server:
    Essentials:
      load: BEFORE
      required: true
    DiscordSRV:
        load: BEFORE
        required: true
    Treasury:
        load: BEFORE
        required: true
    Business:
        load : BEFORE
        required: false
    Realty:
        load: BEFORE
        required: false
    LibertyBans:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      require: false
